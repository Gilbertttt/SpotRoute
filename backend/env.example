PORT=8080
JWT_SECRET=super-secret-key
JWT_EXPIRES_IN=7d

# Option 1: Use DATABASE_URL for Railway/Vercel (recommended for production)
# DATABASE_URL=mysql://user:password@host:port/database

# Option 2: Use individual variables for localhost development
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Gilbert123#
DB_NAME=spotroute

# Note: If DATABASE_URL is set, it will be used instead of individual DB_* variables


